imports:
  - test.depsets.yaml

build_arg_sets:
  py312_cpu:
      CUDA_VERSION: cpu
      PYTHON_VERSION: py312

depsets:
  - name: expand_imported_depset__${PYTHON_VERSION}_${CUDA_VERSION}
    operation: expand
    depsets:
      - general_depset__py311_cpu
      - expanded_depset__py311_cpu
    constraints:
      - requirement_constraints_expand.txt
    output: requirements_compiled_expand_general.txt
    build_arg_sets:
      - py312_cpu
