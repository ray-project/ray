[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "doggos"
version = "0.1.0"
requires-python = ">=3.12"
description = "doggos multimodal ai package"

[tool.setuptools.packages.find]
where = ["."]
include = ["doggos*"]