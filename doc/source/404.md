---
orphan: true
nosearch: true
html_theme.sidebar_secondary.remove: true
---

% Custom 404 page (DOC-945). This source file only exists to register the
% `404` docname so Sphinx builds a `404.html` and fires html-page-context for
% it. The visible content is supplied entirely by the _templates/404.html
% template (which overrides the `body` block), so the text below is not
% rendered. `orphan` suppresses the "not in any toctree" warning under -W;
% `nosearch` keeps the page out of the search index.

# Page not found

The requested page could not be found.
