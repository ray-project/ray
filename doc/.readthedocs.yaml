version: 2
build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"
  commands:
    - python -m pip install --exists-action=w --no-cache-dir -r doc/requirements-doc.txt
    - python -m sphinx -j auto -T -E -b  html -d _build/doctrees -D language=en . $READTHEDOCS_OUTPUT/html