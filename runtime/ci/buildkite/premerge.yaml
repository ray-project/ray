steps:
  - name: "Build wheels"
    command:
      - /bin/bash runtime/ci/build-forge.sh
      - /bin/bash runtime/ci/build-wheels.sh
    agents:
      queue: "large"
    artifact_paths:
      - "/artifats/**/*"
    env:
      BUILDKITE_ARTIFACT_DESTINATION: "s3://bk-premerge-first-jawfish-artifacts/runtime"
