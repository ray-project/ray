steps:
  - name: "Release tests place holder"
    # TODO(can-anyscale): add runtime release tests here.
    command:
      - echo "TODO"
    artifact_paths:
      - "/artifacts/**/*"
    env:
      BUILDKITE_ARTIFACT_DESTINATION: "s3://bk-premerge-first-jawfish-artifacts/runtime"
