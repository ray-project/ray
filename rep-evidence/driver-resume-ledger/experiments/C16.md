# Experiment: C16

C16 was **refuted** by `runs/20260910-042707-C1`. It was tested as part of
[`C1.md`](C1.md), which carries the pre-registration, the counterexample and the
analysis.

The claim was restated after the fix as **C17**; see [`C17.md`](C17.md).

C16 is deliberately left in the ledger as `refuted` rather than edited into
something that passes. The refutation is the evidence that the design doc's
argument for M1 protected the writer and left the reader exposed, and deleting
it would make that argument look sound in retrospect.
