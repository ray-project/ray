增加注释，介绍类的作用，关键feature (慕白/林濯/悟言)
文件名去掉streaming前缀，streaming/src目录下拆分子目录，bazel拆分streaming_lib(慕白)
interface挪到queue中（悟言）
python中queuelink去掉，directcall入口函数区分producer/consumer，queuemananger也可以变轻量 （慕白/悟言）
python中的QueueProducer/Consumer改成DataWriter/Reader，C++也改成DataWriter/Reader（慕白）
去掉StreamingCommon，用组合替代继承，或者改名字（林濯）
streamingtransfer看是否可以去掉，channel的概念保留，后续将queue改为channel (林濯)
Cython shared-lib不打包coreworker，依赖_raylet.so符号，需要试一下 （悟言/慕白)

queue换成channel（低优先级）