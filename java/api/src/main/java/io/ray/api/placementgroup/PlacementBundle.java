package io.ray.api.placementgroup;

/**
 * A placement bundle is used to describe some resources which are packed close together.
 * It contains several types of resources in explicit amounts.
 * Typically, resources in a bundle are packed within one node.
 */
public interface PlacementBundle {
}
