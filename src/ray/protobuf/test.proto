syntax = "proto3";

package ray.rpc;

/// This file is only used for rpc test.

message HelloRequest {
  string who = 1;
  uint64 index = 2;
}

message HelloReply {
  string message = 1;
}

service TestService {
  rpc Hello(HelloRequest) returns (HelloReply);
}