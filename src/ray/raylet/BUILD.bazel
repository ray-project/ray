load("//bazel:ray.bzl", "ray_cc_library")

ray_cc_library(
    name = "wait_manager",
    srcs = ["wait_manager.cc"],
    hdrs = ["wait_manager.h"],
    deps = [
        "//src/ray/common:id",
        "//src/ray/util:container_util",
    ],
    # TODO(edoakes): make this private after moving raylet targets into this BUILD file.
    visibility = ["//visibility:public"],
)
