# Example command to start a cluster with this config:
#
# RAY_FAKE_CLUSTER=1 ray start --autoscaling-config=example.yaml --head --block
#
# Alternatively, you can programmatically create a fake autoscaling cluster
# using ray.cluster_utils.AutoscalingCluster.
cluster_name: fake_multinode
max_workers: 8
provider:
    type: fake_multinode
    use_node_id_as_ip: True
    disable_node_updaters: True
    disable_launch_config_check: True
available_node_types:
    ray.head.default:
        # You must set this manually to your "head" node resources!!
        resources:
            CPU: 8   # <-- set this to num CPUs of current machine
            GPU: 0   # <-- set this to num GPUs of current machine
        node_config: {}
        max_workers: 0
    ray.worker.cpu:
        resources:
            CPU: 1
            object_store_memory: 1000000000
        node_config: {}
        min_workers: 0
        max_workers: 4
    ray.worker.gpu:
        resources:
            CPU: 4
            GPU: 1
            object_store_memory: 1000000000
        node_config: {}
        min_workers: 0
        max_workers: 2
head_node_type: ray.head.default
auth: {}
upscaling_speed: 1.0
idle_timeout_minutes: 0.1
docker: {}
initialization_commands: []
setup_commands: []
head_setup_commands: []
worker_setup_commands: []
head_start_ray_commands: []
worker_start_ray_commands: []
file_mounts: {}
cluster_synced_files: []
file_mounts_sync_continuously: false
rsync_exclude: []
rsync_filter: []
