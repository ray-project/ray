# An unique identifier for the head node and workers of this cluster.
cluster_name: default

# The minimum number of workers nodes to launch in addition to the head
# node. This number should be >= 0.
min_workers: 2

# The maximum number of workers nodes to launch in addition to the head
# node. This takes precedence over min_workers.
max_workers: 4

# The autoscaler will scale up the cluster to this target fraction of resource
# usage. For example, if a cluster of 10 nodes is 100% busy and
# target_utilization is 0.8, it would resize the cluster to 13. This fraction
# can be decreased to increase the aggressiveness of upscaling.
target_utilization_fraction: 0.8

# If a node is idle for this many minutes, it will be removed.
idle_timeout_minutes: 5

# Cloud-provider specific configuration.
provider:
    type: aws
    region: us-west-2
    availability_zone: us-west-2a

# How Ray will authenticate with newly launched nodes.
auth:
    ssh_user: ec2-user
# By default Ray creates a new private keypair, but you can also use your own.
# If you do so, make sure to also set "KeyName" in the head and worker node
# configurations below.
#    ssh_private_key: /path/to/your/key.pem

# Provider-specific config for the head node, e.g. instance type. By default
# Ray will auto-configure unspecified fields such as SubnetId and KeyName.
# For more documentation on available fields, see:
# http://boto3.readthedocs.io/en/latest/reference/services/ec2.html#EC2.ServiceResource.create_instances
head_node:
    InstanceType: m5.large
    ImageId: ami-53903033  # ECS-Optimized (ec2-user)
    # You can provision additional disk space with a conf as follows
    BlockDeviceMappings:
        - DeviceName: /dev/sda1
          Ebs:
              VolumeSize: 50

    # Additional options in the boto docs.

# Provider-specific config for worker nodes, e.g. instance type. By default
# Ray will auto-configure unspecified fields such as SubnetId and KeyName.
# For more documentation on available fields, see:
# http://boto3.readthedocs.io/en/latest/reference/services/ec2.html#EC2.ServiceResource.create_instances
worker_nodes:
    InstanceType: m5.large
    ImageId: ami-53903033  # ECS-Optimized (ec2-user)

    # Run workers on spot by default. Comment this out to use on-demand.
    # InstanceMarketOptions:
    #     MarketType: spot
        # Additional options can be found in the boto docs, e.g.
        #   SpotOptions:
        #       MaxPrice: MAX_HOURLY_PRICE

    # Additional options in the boto docs.

# Files or directories to copy to the head and worker nodes. The format is a
# dictionary from REMOTE_PATH: LOCAL_PATH, e.g.
file_mounts: {
#    "/path1/on/remote/machine": "/path1/on/local/machine",
#    "/path2/on/remote/machine": "/path2/on/local/machine",
}

# List of shell commands to run to set up nodes.
setup_commands:
    - sudo yum upgrade -y docker-engine || true
    # Note: if you're developing Ray, you probably want to create an AMI that
    # has your Ray repo pre-cloned. Then, you can replace the pip installs
    # below with a git checkout <your_sha> (and possibly a recompile).
    # - sudo apt-get install -y docker.io

# Custom commands that will be run on the head node after common setup.
head_setup_commands: []

# Custom commands that will be run on worker nodes after common setup.
worker_setup_commands: []

# Command to start ray on the head node. You don't need to change this.
head_start_ray_commands:
    - sudo kill -SIGUSR1 $(pidof dockerd) || true
    - sudo service docker start
    - sudo usermod -a -G docker ec2-user
    - docker rm -f tune_docker || true
    - docker pull vitchyr/railrl-vitchyr
    - docker run --name tune_docker -d -it -p 6379:6379 -p 8076:8076 --net=host vitchyr/railrl-vitchyr bash
    - docker exec tune_docker pip uninstall -y ray || true
    - docker exec tune_docker  pip install -U https://s3-us-west-2.amazonaws.com/ray-wheels/a59a9e20aff87a5e47d49e2493a40f74b60a1cdb/ray-0.3.0-cp35-cp35m-manylinux1_x86_64.whl
    - docker cp ~/ray_bootstrap_config.yaml tune_docker:/root/ray_bootstrap_config.yaml
    - docker cp ~/ray_bootstrap_key.pem tune_docker:/root/ray_bootstrap_key.pem
    - docker exec tune_docker pip install boto3==1.4.8 # 1.4.8 adds InstanceMarketOptions
    - docker exec tune_docker ray stop
    - docker exec tune_docker ray start --head --redis-port=6379 --object-manager-port=8076  --autoscaling-config=~/ray_bootstrap_config.yaml

# Command to start ray on worker nodes. You don't need to change this.
worker_start_ray_commands:
    - sudo kill -SIGUSR1 $(pidof dockerd) || true
    - sudo service docker start
    - sudo usermod -a -G docker ec2-user
    - docker rm -f tune_docker || true
    - docker pull vitchyr/railrl-vitchyr
    - docker run --name tune_docker -d -it -p 6379:6379 -p 8076:8076 --net=host vitchyr/railrl-vitchyr bash
    - docker exec tune_docker pip uninstall -y ray || true
    - docker exec tune_docker  pip install -U https://s3-us-west-2.amazonaws.com/ray-wheels/a59a9e20aff87a5e47d49e2493a40f74b60a1cdb/ray-0.3.0-cp35-cp35m-manylinux1_x86_64.whl
    - docker exec tune_docker ray stop
    - docker exec tune_docker ray start --redis-address=$RAY_HEAD_IP:6379 --object-manager-port=8076
