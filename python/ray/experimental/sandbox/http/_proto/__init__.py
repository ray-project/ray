"""Vendored gRPC wire contract for the Ray Sandbox gRPC facade.

Only the services, messages, and fields the facade uses are declared. See
``README.md`` for how to regenerate the stubs from the ``.proto`` sources.
"""
