- name: tune_small
  owner:
    mail: "kai@anyscale.com"
    slack: "@tune-team"

  cluster:
    app_config: app_config.yaml
    compute_template: tpl_cpu_small.yaml

  run:
    timeout: 600
    script: workloads/tune_small.py
    results: /tmp/tune_small.json

#  artifacts:
#    output: ~/kf-dev/output.csv

  smoke_test:
    cluster:
      config: cluster_cpu_small.yaml
