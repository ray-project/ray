- name: tune_small
  owner:
    mail: "kai@anyscale.com"
    slack: "@tune-team"

  cluster:
    app_config: app_config.yaml
    compute_template: tpl_cpu_small.yaml

  run:
    timeout: 600
    script: workloads/tune_small.py

#  artifacts:
#    output: ~/kf-dev/output.csv

  smoke_test:
    cluster:
      compute_template: tpl_cpu_smoketest.yaml  # Does not exist yet
