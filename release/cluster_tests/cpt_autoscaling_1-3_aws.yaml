cloud_id: {{env["ANYSCALE_CLOUD_ID"]}}
region: us-west-2

head_node_type:
    name: head_node
    instance_type: m5.xlarge  # 4 CPUs

worker_node_types:
    - name: worker_node
      instance_type: m5.xlarge
      min_worker_nodes: 0
      max_worker_nodes: 2
      use_spot: false
