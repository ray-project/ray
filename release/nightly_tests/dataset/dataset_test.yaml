- name: inference
  owner:
    mail: "core@anyscale.com"
    slack: "@Alex Wu"

  cluster:
    app_config: app_config.yaml
    compute_template: inference.yaml

  run:
    timeout: 600
    prepare: python wait_cluster.py
    script: python inference.py

- name: shuffle_ingest
  owner:
    mail: "core@anyscale.com"
    slack: "@sangcho"

  cluster:
    app_config: shuffle_app_config.yaml
    compute_template: shuffle_multi_node_gpu.yaml

  run:
    timeout: 3600
    script: python shuffle_ingest.py
  
