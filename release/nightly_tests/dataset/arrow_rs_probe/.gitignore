# Probe/experiment output: per-cell logs + summaries, written by run_matrix.py,
# grand_experiment.py and replication_matrix.py. Regenerated by every run and
# tens of MB per run — the analysis lands in arrow_rs_docs/, not here.
matrix_runs/
grand_runs/
replication_runs/
env.sh
