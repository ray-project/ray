cloud_id: {{env["ANYSCALE_CLOUD_ID"]}}
region: us-west-2

max_workers: 15

head_node_type:
    name: head_node
    instance_type: m5.large

worker_node_types:
    - name: worker_node
      instance_type: m5.large
      min_workers: 15
      max_workers: 15
      use_spot: false
