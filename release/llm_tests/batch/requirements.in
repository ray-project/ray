# 4.8 is the first datasets line whose dill cap (<0.4.2) admits the dill the
# llm-cu130 image ships (0.4.1); the lock is constrained to the image, so an
# older pin here is unsatisfiable rather than a silent downgrade.
datasets>=4.8,<5
