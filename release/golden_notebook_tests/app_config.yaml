base_image: "anyscale/ray:1.3.0"
env_vars: {}
debian_packages:
  - curl

python:
  pip_packages:
    - pytest
    - xgboost_ray
    - dask
    - modin
  conda_packages: []

post_build_cmds:
  - pip uninstall -y numpy ray || true
  - pip3 install -U {{ env["RAY_WHEELS"] | default("ray") }}