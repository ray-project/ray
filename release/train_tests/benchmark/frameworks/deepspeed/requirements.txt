# Dependencies for the DeepSpeed LLM benchmark adapter.
#
# NOTE: transformers must be >= 4.51.0 for Qwen3 (`model_type: qwen3`).
# The Ray Train deepspeed_finetune doc example pins transformers==4.36.2, but
# that example defaults to gpt2 and predates Qwen2/Qwen3 — do not copy that pin
# here or AutoModelForCausalLM("Qwen/Qwen3-*") fails with KeyError: 'qwen3'.
torch>=2.3
transformers>=4.51.0
datasets>=3.0
deepspeed>=0.14.0
# GPU utilization / memory sampling in core/metrics.py (optional; no-ops if absent).
nvidia-ml-py>=12.0.0
