#!/bin/bash
echo "docker build -f $DOCKERFILE_PATH -t jcoffi/cluster-anywhere:$DOCKER_TAG --build-arg IMAGETYPE=$DOCKER_TAG"
docker build -f $DOCKERFILE_PATH -t jcoffi/cluster-anywhere:$DOCKER_TAG --build-arg IMAGETYPE=$DOCKER_TAG