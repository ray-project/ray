memory-leak-test-sac:
    stop:
        timesteps_total: 20000
    env:
        ray.rllib.examples.env.random_env.RandomLargeObsSpaceEnv
    run: SAC
    config:
        # Works for both torch and tf.
        framework: tf
        buffer_size: 1000  # use small buffer to catch memory leaks
