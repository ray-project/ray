group: runtime serve tests
steps:
  - label: ":ray-serve: serve: runtime tests"
    instance_type: medium
    commands:
      - bazel run //ci/ray_ci -- //python/ray/anyscale/serve/... serve
    depends_on: servebuild
    job_env: servebuild
