load("@rules_foreign_cc//foreign_cc:defs.bzl", "cmake")

filegroup(
    name = "all_srcs",
    srcs = glob(
        include = ["**"],
    ),
)

cmake(
    name = "libuv_a",
    cache_entries = {
        "BUILD_TESTING": "OFF",
    },
    lib_source = "//:all_srcs",
    visibility = ["//visibility:public"]
)
