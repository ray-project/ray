apiVersion: 1

providers:
  - name: Ray    # A uniquely identifiable name for the provider
    folder: Ray  # The folder where to place the dashboards
    type: file
    options:
      path:  /tmp/ray/metrics/grafana/provisioning/dashboards
